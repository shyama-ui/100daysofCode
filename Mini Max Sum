def miniMaxSum(arr):
    min=arr[0]
    max=arr[0]
    sum=0
    for i in range(len(arr)):
        if(arr[i]>max):
            max=arr[i]
        if(arr[i]<min):
            min=arr[i]
        sum=sum+arr[i]    
    print(str(sum-max)+" "+str(sum-min)) 
